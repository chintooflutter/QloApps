version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      DB_HOST: mysql.railway.internal
      DB_PORT: 3306
      DB_NAME: railway
      DB_USER: root
      DB_PASSWORD: dkXrjPdzupemWVsfPvQNZLpUifGVqzSU
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
