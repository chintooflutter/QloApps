version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      DB_HOST: mysql-apps9-bfftalk143-b56e.l.aivencloud.com
      DB_PORT: 23818      
      DB_NAME: defaultdb
      DB_USER: avnadmin
      DB_PASSWORD: AVNS_4kcxRH1N3YgnDxv3aDo
      APP_ENV: production
      APP_DEBUG: "false"
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
